* Readme

  #+begin_example
  git clone --recurse-submodules git@gitlab.com:janmasrovira/emacs-doom-config.git dotfiles/doom
  -- if using chemacs, stop here. Otherwise use stow to set it up
  cd dotfiles
  stow -n -v 2 tmux # to check if everything is fine
  stow tmux
  #+end_example
